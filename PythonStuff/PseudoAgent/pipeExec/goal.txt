
Write a Java program that behaves like a tiny top/htop:

every 500ms refreshes the screen

shows:

CPU load (system + process)

memory usage

top 10 processes by CPU (or memory)

your current Java process highlighted

supports interactive keys:

q quit

s sort by CPU/mem toggle

r change refresh rate (cycle 250/500/1000ms)

terminates immediately if .ready appears