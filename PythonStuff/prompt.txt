create a folder called HANGMAN %%%)))
set the current working directory to be HANGMAN %%%)))
download the library called <net.bytebuddy:byte-buddy:1.15.3> %%%)))
download the library called <org.ow2.asm:asm:9.8> %%%)))
download the library called <org.ow2.asm:asm-tree:9.8> %%%)))
create a base class called <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <secretWordField.txt> and the content of it should be <<a private String field named secretWord that stores the word the player is trying to guess>> %%%)))
add the field logic from <secretWordField.txt> into <Hangman_spec.json> with the field name <secretWord> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the field <secretWord> from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <guessedLettersField.txt> and the content of it should be <<a private StringBuilder field named guessedLetters that stores all letters the player has already guessed in the order they were entered>> %%%)))
add the field logic from <guessedLettersField.txt> into <Hangman_spec.json> with the field name <guessedLetters> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the field <guessedLetters> from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <remainingAttemptsField.txt> and the content of it should be <<a private int field named remainingAttempts that tracks how many incorrect guesses the player can still make before losing the game>> %%%)))
add the field logic from <remainingAttemptsField.txt> into <Hangman_spec.json> with the field name <remainingAttempts> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the field <remainingAttempts> from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <maxAttemptsField.txt> and the content of it should be <<a private int field named maxAttempts that stores the maximum number of incorrect guesses allowed for a game, used to initialize remainingAttempts and for display purposes>> %%%)))
add the field logic from <maxAttemptsField.txt> into <Hangman_spec.json> with the field name <maxAttempts> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the field <maxAttempts> from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <currentStateField.txt> and the content of it should be <<a private char array field named currentState that represents the current visible state of the secret word, where correctly guessed letters are shown and unknown letters are represented by underscores>> %%%)))
add the field logic from <currentStateField.txt> into <Hangman_spec.json> with the field name <currentState> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the field <currentState> from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <gameOverField.txt> and the content of it should be <<a private boolean field named gameOver that is true when the game has ended either by winning or losing and false while the game is still in progress>> %%%)))
add the field logic from <gameOverField.txt> into <Hangman_spec.json> with the field name <gameOver> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the field <gameOver> from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <wonField.txt> and the content of it should be <<a private boolean field named won that is true if the player has successfully guessed the secret word and false otherwise>> %%%)))
add the field logic from <wonField.txt> into <Hangman_spec.json> with the field name <won> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the field <won> from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <scannerField.txt> and the content of it should be <<a private java.util.Scanner field named scanner that is used to read user input from System.in during the hangman game>> %%%)))
add the field logic from <scannerField.txt> into <Hangman_spec.json> with the field name <scanner> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the field <scanner> from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <wordListField.txt> and the content of it should be <<a private String array field named wordList that contains a small list of possible secret words for the hangman game such as "computer", "java", "hangman", "programming", and "keyboard">> %%%)))
add the field logic from <wordListField.txt> into <Hangman_spec.json> with the field name <wordList> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the field <wordList> from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <initializeGameMethod.txt> and the content of it should be <<a public void method named initializeGame that sets up a new hangman game by performing the following steps in order: 1) if scanner is null then create a new java.util.Scanner reading from System.in and assign it to scanner; 2) if wordList is null or has length 0 then initialize wordList with a few default words such as "computer", "java", "hangman", "programming", and "keyboard"; 3) choose a random index between 0 (inclusive) and wordList.length (exclusive) using java.util.Random and assign the word at that index to secretWord; 4) set maxAttempts to 8; 5) set remainingAttempts to maxAttempts; 6) create a new char array currentState with the same length as secretWord and fill it with underscore characters '_'; 7) create a new StringBuilder and assign it to guessedLetters; 8) set gameOver to false; 9) set won to false; 10) print a welcome message like "Welcome to Hangman!" and then print how many letters the secret word has and how many attempts the player has>> %%%)))
add the logic from <initializeGameMethod.txt> to <Hangman_spec.json> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the <initializeGame> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <displayCurrentStateMethod.txt> and the content of it should be <<a private void method named displayCurrentState that prints the current visible state of the secret word and the letters already guessed by the player; the method should first print a line like "Word: " followed by the characters in currentState separated by spaces, then print a newline, then print a line like "Guessed letters: " followed by the contents of guessedLetters or "(none)" if no letters have been guessed yet, then print another line showing "Remaining attempts: " followed by remainingAttempts>> %%%)))
add the logic from <displayCurrentStateMethod.txt> to <Hangman_spec.json> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the <displayCurrentState> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <processGuessMethod.txt> and the content of it should be <<a private void method named processGuess that takes a single char parameter named guess and updates the game state based on that guess; the method should: 1) convert guess to lowercase; 2) if guessedLetters already contains this character then print a message like "You already guessed that letter." and return without changing attempts; 3) append the guess and a space to guessedLetters; 4) check if secretWord contains the guessed character; if it does, loop over all positions in secretWord and for each index where the character matches guess, set currentState at that index to that character, then print a message like "Good guess!"; 5) if secretWord does not contain the guessed character, decrement remainingAttempts by 1 and print a message like "Wrong guess."; 6) after updating, check if there are no more underscores in currentState; if there are none, set gameOver to true, set won to true, and print a congratulatory message including the secretWord; 7) if remainingAttempts is less than or equal to 0 and won is still false, set gameOver to true and print a message that the player has lost and reveal the secretWord>> %%%)))
add the logic from <processGuessMethod.txt> to <Hangman_spec.json> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the <processGuess> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <gameLoopMethod.txt> and the content of it should be <<a private void method named gameLoop that repeatedly asks the player for letter guesses until the game is over; the method should run a while loop that continues while gameOver is false; inside the loop it should: 1) call displayCurrentState; 2) prompt the user with a line like "Enter a letter: "; 3) read a full line from scanner, trim it, and if the line is empty then print "Please enter a letter." and continue to the next iteration; 4) take the first character of the line as the guess; 5) if the character is not a letter (use Character.isLetter) then print "Please enter a valid letter." and continue; 6) call processGuess with that character; 7) after the loop ends, print a final line like "Game over." and then if won is true print "You won!" otherwise print "You lost." along with the secretWord>> %%%)))
add the logic from <gameLoopMethod.txt> to <Hangman_spec.json> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the <gameLoop> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <mainMethod.txt> and the content of it should be <<a public static void main method with signature public static void main(String[] args) that creates and runs a hangman game; inside main, create a new instance of Hangman, call its initializeGame method, then call its gameLoop method; after the game finishes, close the scanner field if it is not null; wrap the game logic in a try block and in a finally block ensure that the scanner is closed if it was created>> %%%)))
add the logic from <mainMethod.txt> to <Hangman_spec.json> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the <main> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
execute the <Hangman> class %%%)))
