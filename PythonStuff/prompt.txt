create a folder called <ARRAYLIST_PROJECT> %%%)))
set the current working directory to be <ARRAYLIST_PROJECT> %%%)))
download the library called <net.bytebuddy:byte-buddy:1.15.3> %%%)))
download the library called <org.ow2.asm:asm:9.8> %%%)))
download the library called <org.ow2.asm:asm-tree:9.8> %%%)))
create a base class called <Base> %%%)))
create a spec file for the <Base> class %%%)))
create a text file <addFields.txt> and the content of it should be <add two new fields to the Base class: one field is a java.util.ArrayList of java.lang.String named list and the other field is a java.lang.String named extraString. Ensure these are instance fields and will be used by other methods.> %%%)))
add the logic from <addFields.txt> to <Base_spec.json> %%%)))
generate a delegate from <Base_spec.json> %%%)))
clone the <addFields> method from <BaseDelegate> into <Base> %%%)))
create a spec file for the <Base> class %%%)))
create a text file <addStringAndPrint.txt> and the content of it should be <a public void addStringAndPrint() method that first checks if the list field is null and if so initializes it as a new java.util.ArrayList of java.lang.String, then adds the extraString field value to the list, then iterates over all elements of the list and prints each element on its own line using System.out.println.> %%%)))
add the logic from <addStringAndPrint.txt> to <Base_spec.json> %%%)))
generate a delegate from <Base_spec.json> %%%)))
clone the <addStringAndPrint> method from <BaseDelegate> into <Base> %%%)))
create a spec file for the <Base> class %%%)))
create a text file <main.txt> and the content of it should be <a public static void main(String[] args) method that creates a new instance of Base, sets the extraString field on that instance to some sample text such as a greeting message, ensures the list field is either left null or initialized as an empty java.util.ArrayList of java.lang.String, then calls the addStringAndPrint() method on the instance so that the extraString value is added to the list and all elements of the list are printed to the console.> %%%)))
add the logic from <main.txt> to <Base_spec.json> %%%)))
generate a delegate from <Base_spec.json> %%%)))
clone the <main> method from <BaseDelegate> into <Base> %%%)))
create a spec file for the <Base> class %%%)))
execute the <Base> class %%%)))
