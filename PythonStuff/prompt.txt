create a folder called <ARRAYLIST_TASK> %%%)))
set the current working directory to be <ARRAYLIST_TASK> %%%)))
download the library called <net.bytebuddy:byte-buddy:1.15.3> %%%)))
download the library called <org.ow2.asm:asm:9.8> %%%)))
download the library called <org.ow2.asm:asm-tree:9.8> %%%)))
create a base class called <Base> %%%)))
create a spec file for the <Base> class %%%)))
create a text file called <arrayListField.txt> and the content of it should be <a new field named listOfStrings that represents a resizable list of text values using the standard Java dynamic list type for strings> %%%)))
add the logic from <arrayListField.txt> to <Base_spec.json> %%%)))
generate a delegate from <Base_spec.json> %%%)))
clone the <listOfStrings> field from <BaseDelegate> into <Base> %%%)))
create a spec file for the <Base> class %%%)))
create a text file called <singleStringField.txt> and the content of it should be <a new field named extraString that represents a single standalone text value that will later be added into the listOfStrings collection> %%%)))
add the logic from <singleStringField.txt> to <Base_spec.json> %%%)))
generate a delegate from <Base_spec.json> %%%)))
clone the <extraString> field from <BaseDelegate> into <Base> %%%)))
create a spec file for the <Base> class %%%)))
create a text file called <populateAndPrintList.txt> and the content of it should be <a public method named populateAndPrintList with no parameters and no return value that first checks whether the listOfStrings field is already created and if it is not created yet it initializes it as an empty dynamic list of strings then adds the extraString field value into this list and finally iterates over all elements in listOfStrings and prints each element on its own line to the console> %%%)))
add the logic from <populateAndPrintList.txt> to <Base_spec.json> %%%)))
generate a delegate from <Base_spec.json> %%%)))
clone the <populateAndPrintList> method from <BaseDelegate> into <Base> %%%)))
create a spec file for the <Base> class %%%)))
create a text file called <mainMethod.txt> and the content of it should be <a public static void main method with a string array parameter that creates a new instance of the Base class assigns a meaningful text value to the extraString field such as a short greeting or label then ensures that the listOfStrings field is initialized as an empty dynamic list of strings if it is still null optionally adds a couple of additional hard coded text entries into the listOfStrings field and finally calls the populateAndPrintList method on the created Base instance so that all elements of the list including the extraString value are printed to the console> %%%)))
add the logic from <mainMethod.txt> to <Base_spec.json> %%%)))
generate a delegate from <Base_spec.json> %%%)))
clone the <main> method from <BaseDelegate> into <Base> %%%)))
create a spec file for the <Base> class %%%)))
execute the <Base> class %%%)))
