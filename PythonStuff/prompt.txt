create a folder called <HANGMAN> %%%)))
set the current working directory to be <HANGMAN> %%%)))
download the library called <net.bytebuddy:byte-buddy:1.15.3> %%%)))
download the library called <org.ow2.asm:asm:9.8> %%%)))
download the library called <org.ow2.asm:asm-tree:9.8> %%%)))
create a base class called <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <secretWordField.txt> and the content of it should be <<a public String field named <secretWord> that stores the word the player is trying to guess>> %%%)))
add the field logic from <secretWordField.txt> into the spec file <Hangman_spec.json> for the field named <secretWord> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the <secretWord> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <guessedLettersField.txt> and the content of it should be <<a public String field named <guessedLetters> that stores all letters the player has already guessed, concatenated together in order of guessing>> %%%)))
add the field logic from <guessedLettersField.txt> into the spec file <Hangman_spec.json> for the field named <guessedLetters> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the <guessedLetters> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <remainingAttemptsField.txt> and the content of it should be <<a public int field named <remainingAttempts> that stores how many incorrect guesses the player can still make before losing the game>> %%%)))
add the field logic from <remainingAttemptsField.txt> into the spec file <Hangman_spec.json> for the field named <remainingAttempts> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the <remainingAttempts> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <gameOverField.txt> and the content of it should be <<a public boolean field named <gameOver> that is true when the game has ended either by winning or losing, and false while the game is still in progress>> %%%)))
add the field logic from <gameOverField.txt> into the spec file <Hangman_spec.json> for the field named <gameOver> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the <gameOver> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <initializeGameMethod.txt> and the content of it should be <<a public void method named <initializeGame> with no parameters that initializes a new hangman game by performing the following steps in order: 
1) set <secretWord> to a randomly chosen word from a small built-in list of lowercase words such as <java>, <hangman>, <computer>, and <program>. use java.util.Random to pick an index into the list. 
2) set <guessedLetters> to an empty String. 
3) set <remainingAttempts> to 6 so the player has six incorrect guesses available. 
4) set <gameOver> to false.>> %%%)))
add the logic from <initializeGameMethod.txt> to <Hangman_spec.json> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the <initializeGame> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <getMaskedWordMethod.txt> and the content of it should be <<a public String method named <getMaskedWord> with no parameters that returns a masked representation of <secretWord> based on the letters in <guessedLetters>. the method should:
1) create a StringBuilder. 
2) for each character in <secretWord>, check if that character appears anywhere in <guessedLetters>. 
3) if the character has been guessed, append the character itself to the StringBuilder. 
4) if the character has not been guessed, append an underscore character '_' instead. 
5) after processing all characters, convert the StringBuilder to a String and return it.>> %%%)))
add the logic from <getMaskedWordMethod.txt> to <Hangman_spec.json> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the <getMaskedWord> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <isWordGuessedMethod.txt> and the content of it should be <<a public boolean method named <isWordGuessed> with no parameters that returns true if the player has successfully guessed all letters in <secretWord>, and false otherwise. the method should:
1) call <getMaskedWord> to obtain the current masked word. 
2) compare the masked word to <secretWord> using String equality. 
3) if they are equal, return true, otherwise return false.>> %%%)))
add the logic from <isWordGuessedMethod.txt> to <Hangman_spec.json> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the <isWordGuessed> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <processGuessMethod.txt> and the content of it should be <<a public void method named <processGuess> that takes a single parameter <char guess>. this method processes one letter guess from the player and updates the game state fields. the method should:
1) if <gameOver> is true, immediately return without doing anything. 
2) convert <guess> to lowercase. 
3) if <guessedLetters> already contains this lowercase character, simply return without changing attempts or state. 
4) otherwise, append the lowercase character to <guessedLetters>. 
5) check if <secretWord> contains the guessed character. 
6) if <secretWord> does not contain the guessed character, decrement <remainingAttempts> by 1. 
7) after updating attempts, if <isWordGuessed> returns true, set <gameOver> to true. 
8) else if <remainingAttempts> is less than or equal to 0, set <gameOver> to true as well.>> %%%)))
add the logic from <processGuessMethod.txt> to <Hangman_spec.json> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the <processGuess> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <printGameStateMethod.txt> and the content of it should be <<a public void method named <printGameState> with no parameters that prints the current state of the game to System.out. the method should:
1) print a line that shows the masked word by calling <getMaskedWord>, prefixed with the text <Word: >. 
2) print a line that shows the letters guessed so far in <guessedLetters>, prefixed with the text <Guessed letters: >. 
3) print a line that shows the number of remaining attempts from <remainingAttempts>, prefixed with the text <Remaining attempts: >. 
4) if <gameOver> is true and <isWordGuessed> returns true, print a line saying <You won! The word was: > followed by <secretWord>. 
5) else if <gameOver> is true and <isWordGuessed> returns false, print a line saying <You lost! The word was: > followed by <secretWord>.>> %%%)))
add the logic from <printGameStateMethod.txt> to <Hangman_spec.json> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the <printGameState> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <mainMethod.txt> and the content of it should be <<a public static void method named <main> with parameter <String[] args> that runs the hangman game in the console. the method should:
1) create a new instance of <Hangman> and store it in a local variable. 
2) call <initializeGame> on that instance. 
3) create a java.util.Scanner that reads from System.in. 
4) in a loop that continues while the instance field <gameOver> is false, do the following steps: 
   a) call <printGameState> on the instance. 
   b) print a prompt line such as <Enter a letter: >. 
   c) read a line of input from the scanner. 
   d) if the line is empty, continue the loop without processing. 
   e) otherwise, take the first character of the line as the guess. 
   f) call <processGuess> on the instance with that character. 
5) after the loop ends, call <printGameState> one final time so the player sees the final result. 
6) close the Scanner.>> %%%)))
add the logic from <mainMethod.txt> to <Hangman_spec.json> %%%)))
generate a delegate from <Hangman_spec.json> %%%)))
clone the <main> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
execute the <Hangman> class %%%)))
