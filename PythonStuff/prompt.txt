create a folder called <HANGMAN> %%%)))
set the current working directory to be <HANGMAN> %%%)))
download the library called <net.bytebuddy:byte-buddy:1.15.3> %%%)))
download the library called <org.ow2.asm:asm:9.8> %%%)))
download the library called <org.ow2.asm:asm-tree:9.8> %%%)))
create a base class called <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <secretWordField.txt> and the content of it should be <create a public String field named secretWord that stores the word the player is trying to guess in the hangman game> %%%)))
add the field logic from <secretWordField.txt> into <<Hangman_spec.json>> for the field <secretWord> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <secretWord> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <guessedLettersField.txt> and the content of it should be <create a public java.util.HashSet<Character> field named guessedLetters that stores all letters the player has already guessed in the hangman game> %%%)))
add the field logic from <guessedLettersField.txt> into <<Hangman_spec.json>> for the field <guessedLetters> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <guessedLetters> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <remainingAttemptsField.txt> and the content of it should be <create a public int field named remainingAttempts that stores how many incorrect guesses the player can still make before losing the hangman game> %%%)))
add the field logic from <remainingAttemptsField.txt> into <<Hangman_spec.json>> for the field <remainingAttempts> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <remainingAttempts> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <maxAttemptsField.txt> and the content of it should be <create a public int field named maxAttempts that stores the maximum number of incorrect guesses allowed in the hangman game and is used to determine which hangman drawing stage to show> %%%)))
add the field logic from <maxAttemptsField.txt> into <<Hangman_spec.json>> for the field <maxAttempts> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <maxAttempts> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <scannerField.txt> and the content of it should be <create a public java.util.Scanner field named scanner that is used to read user input from System.in for the hangman game> %%%)))
add the field logic from <scannerField.txt> into <<Hangman_spec.json>> for the field <scanner> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <scanner> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <wordListField.txt> and the content of it should be <create a public java.util.List<String> field named wordList that contains a list of possible secret words for the hangman game> %%%)))
add the field logic from <wordListField.txt> into <<Hangman_spec.json>> for the field <wordList> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <wordList> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <randomField.txt> and the content of it should be <create a public java.util.Random field named random that is used to randomly select a secret word from the wordList for the hangman game> %%%)))
add the field logic from <randomField.txt> into <<Hangman_spec.json>> for the field <random> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <random> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <currentDisplayWordField.txt> and the content of it should be <create a public String field named currentDisplayWord that stores the current masked representation of the secretWord where unguessed letters are shown as underscores and correctly guessed letters are revealed> %%%)))
add the field logic from <currentDisplayWordField.txt> into <<Hangman_spec.json>> for the field <currentDisplayWord> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <currentDisplayWord> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <gameOverField.txt> and the content of it should be <create a public boolean field named gameOver that indicates whether the current hangman game has ended either by winning or losing> %%%)))
add the field logic from <gameOverField.txt> into <<Hangman_spec.json>> for the field <gameOver> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <gameOver> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <initializeGameMethod.txt> and the content of it should be <create a public void method named initializeGame that sets up a new hangman game by performing the following steps in order: if scanner is null then assign scanner to a new java.util.Scanner using System.in; if random is null then assign random to a new java.util.Random; if wordList is null then create a new java.util.ArrayList<String> and assign it to wordList, then add several example words such as "computer", "java", "hangman", "puzzle", and "keyboard" to wordList; set maxAttempts to 7 to represent the total number of hangman drawing stages; set remainingAttempts to maxAttempts; use random to select a random index from 0 inclusive to wordList.size() exclusive and assign the word at that index to secretWord; convert secretWord to lowercase to make guessing case insensitive; clear guessedLetters by assigning a new java.util.HashSet<Character> to it; build currentDisplayWord by creating a StringBuilder, appending an underscore character for each character in secretWord, and then assigning the resulting String to currentDisplayWord; set gameOver to false so that the game loop can start> %%%)))
add the logic from <initializeGameMethod.txt> to <<Hangman_spec.json>> %%%)))
generate a method delegate from <<Hangman_spec.json>> %%%)))
clone the <initializeGame> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <updateDisplayWordMethod.txt> and the content of it should be <create a public void method named updateDisplayWord that rebuilds the currentDisplayWord string based on the secretWord and the letters stored in guessedLetters by doing the following: create a new StringBuilder; for each character c in secretWord, if guessedLetters contains c then append c to the StringBuilder, otherwise append an underscore character; after the loop convert the StringBuilder to a String and assign it to currentDisplayWord> %%%)))
add the logic from <updateDisplayWordMethod.txt> to <<Hangman_spec.json>> %%%)))
generate a method delegate from <<Hangman_spec.json>> %%%)))
clone the <updateDisplayWord> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <isWordGuessedMethod.txt> and the content of it should be <create a public boolean method named isWordGuessed that returns true if the player has successfully guessed the entire secretWord and false otherwise by comparing currentDisplayWord and secretWord; if currentDisplayWord is not null and currentDisplayWord.equals(secretWord) is true then return true, otherwise return false> %%%)))
add the logic from <isWordGuessedMethod.txt> to <<Hangman_spec.json>> %%%)))
generate a method delegate from <<Hangman_spec.json>> %%%)))
clone the <isWordGuessed> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <drawHangmanMethod.txt> and the content of it should be <create a public void method named drawHangman that prints a textual description of the hangman drawing stage based on how many incorrect attempts have been used; compute an int variable usedAttempts as maxAttempts minus remainingAttempts; then use a series of if or switch statements to print different ASCII described stages of the gallows and hangman without using actual ASCII art characters, only describing them in text; for example when usedAttempts is 0 print a description like "Stage 0: empty gallows with no body parts"; when usedAttempts is 1 print "Stage 1: gallows with the head drawn"; when usedAttempts is 2 print "Stage 2: gallows with head and torso"; when usedAttempts is 3 print "Stage 3: gallows with head, torso, and one arm"; when usedAttempts is 4 print "Stage 4: gallows with head, torso, and both arms"; when usedAttempts is 5 print "Stage 5: gallows with head, torso, both arms, and one leg"; when usedAttempts is 6 print "Stage 6: gallows with head, torso, both arms, and both legs"; when usedAttempts is greater than or equal to 7 print "Stage 7: full hangman figure completed on the gallows"; always print the description using System.out.println so the player can see the current hangman drawing state> %%%)))
add the logic from <drawHangmanMethod.txt> to <<Hangman_spec.json>> %%%)))
generate a method delegate from <<Hangman_spec.json>> %%%)))
clone the <drawHangman> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <printGameStateMethod.txt> and the content of it should be <create a public void method named printGameState that shows the current status of the hangman game to the player by performing the following steps: call drawHangman to show the current hangman drawing stage; print a line that shows the currentDisplayWord with spaces between each character so the player can see which letters have been revealed and which are still hidden; print the number of remainingAttempts; print the list of guessed letters by iterating over guessedLetters and printing them separated by spaces; ensure all output is printed using System.out.println and System.out.print so that the console clearly shows the current game state> %%%)))
add the logic from <printGameStateMethod.txt> to <<Hangman_spec.json>> %%%)))
generate a method delegate from <<Hangman_spec.json>> %%%)))
clone the <printGameState> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <processGuessMethod.txt> and the content of it should be <create a public void method named processGuess that reads a single letter guess from the player and updates the game state; inside the method print a prompt asking the player to enter a single letter; read a line of input from scanner into a String variable; if the input is null or has length 0 then print a message that the input is invalid and return; take the first character of the trimmed lowercase version of the input as the guessed character; if guessedLetters already contains this character then print a message that the letter was already guessed and return; otherwise add the character to guessedLetters; check whether secretWord contains this character by iterating over its characters; if the character is found in secretWord then call updateDisplayWord and print a message that the guess was correct; if the character is not found then decrement remainingAttempts by 1 and print a message that the guess was incorrect and show how many attempts remain; after updating remainingAttempts check if remainingAttempts is less than or equal to 0, and if so set gameOver to true and print a message that the player has lost and reveal the secretWord; also after updating currentDisplayWord check if isWordGuessed returns true, and if so set gameOver to true and print a congratulatory message that the player has won and reveal the secretWord> %%%)))
add the logic from <processGuessMethod.txt> to <<Hangman_spec.json>> %%%)))
generate a method delegate from <<Hangman_spec.json>> %%%)))
clone the <processGuess> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <playGameMethod.txt> and the content of it should be <create a public void method named playGame that runs a full hangman game loop; inside the method call initializeGame to set up the fields; then enter a while loop that continues as long as gameOver is false; at the start of each loop iteration call printGameState to show the current hangman drawing and word progress; then call processGuess to read and handle the player guess; after processGuess returns, if gameOver is true then break out of the loop; after the loop ends print a final message indicating that the game is over and thank the player for playing hangman> %%%)))
add the logic from <playGameMethod.txt> to <<Hangman_spec.json>> %%%)))
generate a method delegate from <<Hangman_spec.json>> %%%)))
clone the <playGame> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <mainMethod.txt> and the content of it should be <create a public static void main(String[] args) method that serves as the entry point for the hangman game; inside main create a new instance of Hangman and assign it to a local variable; then call the playGame method on that instance so that the game starts when the program is run> %%%)))
add the logic from <mainMethod.txt> to <<Hangman_spec.json>> %%%)))
generate a method delegate from <<Hangman_spec.json>> %%%)))
clone the <main> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
execute the <Hangman> class %%%)))
