create a folder called <HANGMAN> %%%)))
set the current working directory to be <HANGMAN> %%%)))
download the library called <net.bytebuddy:byte-buddy:1.15.3> %%%)))
download the library called <org.ow2.asm:asm:9.8> %%%)))
download the library called <org.ow2.asm:asm-tree:9.8> %%%)))
create a base class called <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <secretWordField.txt> and the content of it should be <create a new public String field named secretWord that stores the word the player is trying to guess in the hangman game> %%%)))
add the field logic from <secretWordField.txt> into <<Hangman_spec.json>> for the field <secretWord> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <secretWord> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <guessedLettersField.txt> and the content of it should be <create a new public java.util.HashSet<Character> field named guessedLetters that stores all letters the player has already guessed in the hangman game> %%%)))
add the field logic from <guessedLettersField.txt> into <<Hangman_spec.json>> for the field <guessedLetters> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <guessedLetters> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <remainingAttemptsField.txt> and the content of it should be <create a new public int field named remainingAttempts that stores how many incorrect guesses the player can still make before losing the hangman game> %%%)))
add the field logic from <remainingAttemptsField.txt> into <<Hangman_spec.json>> for the field <remainingAttempts> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <remainingAttempts> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <maxAttemptsField.txt> and the content of it should be <create a new public int field named maxAttempts that stores the maximum number of incorrect guesses allowed in the hangman game and is used to control the drawing stages of the hangman> %%%)))
add the field logic from <maxAttemptsField.txt> into <<Hangman_spec.json>> for the field <maxAttempts> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <maxAttempts> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <currentStateField.txt> and the content of it should be <create a new public StringBuilder field named currentState that stores the current visible state of the secret word where correctly guessed letters are shown and unknown letters are represented by underscores> %%%)))
add the field logic from <currentStateField.txt> into <<Hangman_spec.json>> for the field <currentState> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <currentState> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <scannerField.txt> and the content of it should be <create a new public java.util.Scanner field named scanner that is used to read user input from System.in during the hangman game> %%%)))
add the field logic from <scannerField.txt> into <<Hangman_spec.json>> for the field <scanner> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <scanner> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <wordsField.txt> and the content of it should be <create a new public java.util.List<String> field named words that stores a list of possible secret words for the hangman game from which one will be chosen at random> %%%)))
add the field logic from <wordsField.txt> into <<Hangman_spec.json>> for the field <words> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <words> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <randomField.txt> and the content of it should be <create a new public java.util.Random field named random that is used to randomly select the secret word from the words list in the hangman game> %%%)))
add the field logic from <randomField.txt> into <<Hangman_spec.json>> for the field <random> %%%)))
generate a field delegate from <<Hangman_spec.json>> %%%)))
clone the <random> field from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <initializeGameMethod.txt> and the content of it should be <create a public void method named initializeGame that sets up the hangman game by initializing the words list with several example words like "computer", "java", "hangman", and "puzzle", initializing the random field, choosing a random word from the list and assigning it to secretWord, setting maxAttempts to a fixed value such as 7, setting remainingAttempts equal to maxAttempts, creating a new HashSet for guessedLetters, creating a new StringBuilder for currentState with the same length as secretWord where each character is an underscore, and initializing the scanner field with a new Scanner reading from System.in> %%%)))
add the logic from <initializeGameMethod.txt> to <<Hangman_spec.json>> %%%)))
generate a delegate from <<Hangman_spec.json>> %%%)))
clone the <initializeGame> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <drawHangmanMethod.txt> and the content of it should be <create a public void method named drawHangman that prints an ASCII description of the hangman gallows and body based on how many incorrect attempts have been used which is maxAttempts minus remainingAttempts; the method should compute an integer stage equal to maxAttempts minus remainingAttempts and then use a series of if or switch statements to print lines that describe the gallows and the hangman figure in text form such as "print the base of the gallows", "print the vertical post", "print the horizontal beam", "print the rope", "print the head", "print the torso", "print the arms", and "print the legs"; instead of drawing with ASCII art characters, describe each part in plain text on separate lines like 'HEAD: O', 'TORSO: |', 'LEFT ARM: /', 'RIGHT ARM: \\', 'LEFT LEG: /', 'RIGHT LEG: \\', and ensure that more body parts appear as the stage number increases until the full hangman is described when the player has used all attempts> %%%)))
add the logic from <drawHangmanMethod.txt> to <<Hangman_spec.json>> %%%)))
generate a delegate from <<Hangman_spec.json>> %%%)))
clone the <drawHangman> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <printCurrentStateMethod.txt> and the content of it should be <create a public void method named printCurrentState that prints a user friendly view of the currentState field by printing a label like "Word: " followed by the characters of currentState separated by spaces, then printing the remainingAttempts value with a label like "Remaining attempts: ", and finally printing the contents of guessedLetters with a label like "Guessed letters: " where the letters are printed in sorted order or in the order they were guessed> %%%)))
add the logic from <printCurrentStateMethod.txt> to <<Hangman_spec.json>> %%%)))
generate a delegate from <<Hangman_spec.json>> %%%)))
clone the <printCurrentState> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <processGuessMethod.txt> and the content of it should be <create a public void method named processGuess that takes a single char parameter named guess and handles one player guess in the hangman game; the method should first convert guess to lowercase, check if guessedLetters already contains the guess and if so print a message that the letter was already guessed and return; otherwise add the guess to guessedLetters; then check if secretWord contains the guessed character and if it does, loop over all positions of secretWord and for each position where the character matches guess, update the corresponding index in currentState to that character and print a message that the guess was correct; if the secretWord does not contain the guessed character, decrement remainingAttempts by one and print a message that the guess was incorrect and show how many attempts remain; this method should not end the game but only update the state> %%%)))
add the logic from <processGuessMethod.txt> to <<Hangman_spec.json>> %%%)))
generate a delegate from <<Hangman_spec.json>> %%%)))
clone the <processGuess> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <isWordGuessedMethod.txt> and the content of it should be <create a public boolean method named isWordGuessed that returns true if the player has successfully guessed the entire secret word; this method should compare currentState to secretWord by checking that there are no underscore characters remaining in currentState or by converting currentState to a String and comparing it to secretWord; if the word is fully revealed it should return true otherwise it should return false> %%%)))
add the logic from <isWordGuessedMethod.txt> to <<Hangman_spec.json>> %%%)))
generate a delegate from <<Hangman_spec.json>> %%%)))
clone the <isWordGuessed> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <gameLoopMethod.txt> and the content of it should be <create a public void method named gameLoop that runs the main loop of the hangman game; this method should repeatedly do the following while remainingAttempts is greater than zero and isWordGuessed returns false: call drawHangman to show the current hangman description, call printCurrentState to show the word and guessed letters, prompt the user with a message like "Enter a letter: ", read a line from scanner, validate that the input is a single alphabetic character, and if it is not valid print an error message and continue to the next iteration without changing the game state; if the input is valid, take the first character, pass it to processGuess, and then continue the loop; after the loop ends, if isWordGuessed returns true print a congratulatory message that includes the secretWord, otherwise print a losing message that reveals the secretWord and then call drawHangman one last time to show the final hangman description> %%%)))
add the logic from <gameLoopMethod.txt> to <<Hangman_spec.json>> %%%)))
generate a delegate from <<Hangman_spec.json>> %%%)))
clone the <gameLoop> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
create a text file <mainMethod.txt> and the content of it should be <create a public static void main(String[] args) method that serves as the entry point for the hangman game; inside main, create a new instance of Hangman, call its initializeGame method to set up the fields, then print a welcome message like "Welcome to Hangman!", and finally call the gameLoop method on the instance to start the interactive game; after the game loop finishes, close the scanner field to release system resources> %%%)))
add the logic from <mainMethod.txt> to <<Hangman_spec.json>> %%%)))
generate a delegate from <<Hangman_spec.json>> %%%)))
clone the <main> method from <HangmanDelegate> into <Hangman> %%%)))
create a spec file for the <Hangman> class %%%)))
execute the <Hangman> class %%%)))
