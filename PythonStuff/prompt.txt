create a folder called <GAME2048> %%%)))
set the current working directory to be <GAME2048> %%%)))
download the library called <net.bytebuddy:byte-buddy:1.15.3> %%%)))
download the library called <org.ow2.asm:asm:9.8> %%%)))
download the library called <org.ow2.asm:asm-tree:9.8> %%%)))
create a base class called <Game2048> %%%)))
create a spec file for the <Game2048> class %%%)))
create a text file <initBoard.txt> and the content of it should be <create a 4x4 integer grid field called <board> if it does not exist and implement a public method <initBoard> that sets all cells of <board> to 0 and then randomly selects two empty cells and assigns each either 2 or 4 with equal probability> %%%)))
add the logic from <initBoard.txt> to <Game2048_spec.json> %%%)))
generate a delegate from <Game2048_spec.json> %%%)))
clone the <board> field from <Game2048Delegate> into <Game2048> %%%)))
clone the <initBoard> method from <Game2048Delegate> into <Game2048> %%%)))
create a spec file for the <Game2048> class %%%)))
create a text file <printBoard.txt> and the content of it should be <implement a public method <printBoard> that prints the current state of the <board> field as a 4x4 grid to standard output with each cell width padded to at least 5 characters and empty cells shown as a dot character> %%%)))
add the logic from <printBoard.txt> to <Game2048_spec.json> %%%)))
generate a delegate from <Game2048_spec.json> %%%)))
clone the <printBoard> method from <Game2048Delegate> into <Game2048> %%%)))
create a spec file for the <Game2048> class %%%)))
create a text file <compressRowLeft.txt> and the content of it should be <implement a private helper method <compressRowLeft> that takes an integer array parameter <row> of length 4 and returns a new integer array of length 4 where all nonzero values of <row> are shifted as far left as possible preserving their relative order and all remaining cells on the right are filled with 0> %%%)))
add the logic from <compressRowLeft.txt> to <Game2048_spec.json> %%%)))
generate a delegate from <Game2048_spec.json> %%%)))
clone the <compressRowLeft> method from <Game2048Delegate> into <Game2048> %%%)))
create a spec file for the <Game2048> class %%%)))
create a text file <mergeRowLeft.txt> and the content of it should be <implement a private helper method <mergeRowLeft> that takes an integer array parameter <row> of length 4 and returns a new integer array of length 4 where adjacent equal nonzero tiles are merged from left to right by doubling the left tile value and setting the right tile to 0 and each tile may merge at most once per call then call <compressRowLeft> on the intermediate result before returning it> %%%)))
add the logic from <mergeRowLeft.txt> to <Game2048_spec.json> %%%)))
generate a delegate from <Game2048_spec.json> %%%)))
clone the <mergeRowLeft> method from <Game2048Delegate> into <Game2048> %%%)))
create a spec file for the <Game2048> class %%%)))
create a text file <moveLeft.txt> and the content of it should be <implement a public method <moveLeft> that applies the 2048 move left operation to the <board> field by for each row extracting its 4 values into a temporary array calling <compressRowLeft> then <mergeRowLeft> and writing the result back into the row and returning a boolean indicating whether any tile changed position or value during the move> %%%)))
add the logic from <moveLeft.txt> to <Game2048_spec.json> %%%)))
generate a delegate from <Game2048_spec.json> %%%)))
clone the <moveLeft> method from <Game2048Delegate> into <Game2048> %%%)))
create a spec file for the <Game2048> class %%%)))
create a text file <moveRight.txt> and the content of it should be <implement a public method <moveRight> that performs the 2048 move right operation on the <board> field by for each row reversing it into a temporary array calling <compressRowLeft> then <mergeRowLeft> on that array reversing the result back and storing it into the row and returning a boolean indicating whether any tile changed> %%%)))
add the logic from <moveRight.txt> to <Game2048_spec.json> %%%)))
generate a delegate from <Game2048_spec.json> %%%)))
clone the <moveRight> method from <Game2048Delegate> into <Game2048> %%%)))
create a spec file for the <Game2048> class %%%)))
create a text file <moveUp.txt> and the content of it should be <implement a public method <moveUp> that performs the 2048 move up operation on the <board> field by treating each column as a 4 element array extracting it top to bottom calling <compressRowLeft> then <mergeRowLeft> on that array and writing the result back into the column and returning a boolean indicating whether any tile changed> %%%)))
add the logic from <moveUp.txt> to <Game2048_spec.json> %%%)))
generate a delegate from <Game2048_spec.json> %%%)))
clone the <moveUp> method from <Game2048Delegate> into <Game2048> %%%)))
create a spec file for the <Game2048> class %%%)))
create a text file <moveDown.txt> and the content of it should be <implement a public method <moveDown> that performs the 2048 move down operation on the <board> field by treating each column as a 4 element array extracting it bottom to top calling <compressRowLeft> then <mergeRowLeft> on that array reversing the result back to top to bottom order and writing it into the column and returning a boolean indicating whether any tile changed> %%%)))
add the logic from <moveDown.txt> to <Game2048_spec.json> %%%)))
generate a delegate from <Game2048_spec.json> %%%)))
clone the <moveDown> method from <Game2048Delegate> into <Game2048> %%%)))
create a spec file for the <Game2048> class %%%)))
create a text file <addRandomTile.txt> and the content of it should be <implement a public method <addRandomTile> that scans the <board> field for all empty cells with value 0 chooses one uniformly at random and assigns it either 2 or 4 with probability 0 point 9 for 2 and 0 point 1 for 4 and returns true if a tile was added or false if there were no empty cells> %%%)))
add the logic from <addRandomTile.txt> to <Game2048_spec.json> %%%)))
generate a delegate from <Game2048_spec.json> %%%)))
clone the <addRandomTile> method from <Game2048Delegate> into <Game2048> %%%)))
create a spec file for the <Game2048> class %%%)))
create a text file <hasMoves.txt> and the content of it should be <implement a public method <hasMoves> that returns true if there is at least one legal move remaining on the <board> field by checking for any empty cell with value 0 or any horizontally or vertically adjacent pair of equal tiles and false otherwise> %%%)))
add the logic from <hasMoves.txt> to <Game2048_spec.json> %%%)))
generate a delegate from <Game2048_spec.json> %%%)))
clone the <hasMoves> method from <Game2048Delegate> into <Game2048> %%%)))
create a spec file for the <Game2048> class %%%)))
create a text file <playLoop.txt> and the content of it should be <implement a public method <playLoop> that uses standard input to read single character commands w a s d for up left down right respectively and q to quit initializes the game by calling <initBoard> and <addRandomTile> twice then in a loop prints the board using <printBoard> reads a command applies the corresponding move method if it returns true then calls <addRandomTile> and after each move checks <hasMoves> and if it returns false prints a game over message and exits the loop> %%%)))
add the logic from <playLoop.txt> to <Game2048_spec.json> %%%)))
generate a delegate from <Game2048_spec.json> %%%)))
clone the <playLoop> method from <Game2048Delegate> into <Game2048> %%%)))
create a spec file for the <Game2048> class %%%)))
create a text file <main.txt> and the content of it should be <implement a public static void main(String[] args) method that creates a new instance of <Game2048> and calls its <playLoop> method to start the interactive 2048 game> %%%)))
add the logic from <main.txt> to <Game2048_spec.json> %%%)))
generate a delegate from <Game2048_spec.json> %%%)))
clone the <main> method from <Game2048Delegate> into <Game2048> %%%)))
execute the <Game2048> class %%%)))
