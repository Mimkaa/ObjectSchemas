create a folder called <PROJECT> %%%)))
set the current working directory to be <PROJECT> %%%)))
download the library called <net.bytebuddy:byte-buddy:1.15.3> %%%)))
download the library called <org.ow2.asm:asm:9.8> %%%)))
download the library called <org.ow2.asm:asm-tree:9.8> %%%)))
create a base class called <Base> %%%)))
create a spec file for the <Base> class %%%)))
create a text file <stringListField.txt> and the content of it should be <<a non static field of type java.util.ArrayList<String> named stringList that will store multiple string elements>> %%%)))
add the logic from <stringListField.txt> to <<Base_spec.json>> %%%)))
generate a delegate from <<Base_spec.json>> %%%)))
clone the <stringList> field from <<BaseDelegate>> into <<Base>> %%%)))
create a spec file for the <Base> class %%%)))
create a text file <singleStringField.txt> and the content of it should be <<a non static field of type String named extraString that will store a single additional string value>> %%%)))
add the logic from <singleStringField.txt> to <<Base_spec.json>> %%%)))
generate a delegate from <<Base_spec.json>> %%%)))
clone the <extraString> field from <<BaseDelegate>> into <<Base>> %%%)))
create a spec file for the <Base> class %%%)))
create a text file <addAndPrintMethod.txt> and the content of it should be <<a non static method named addAndPrint with no parameters and no return value that does the following steps in order: first ensure that the stringList field is initialized by checking if it is null and if so creating a new java.util.ArrayList<String> and assigning it to stringList, then add the extraString field value to the stringList list, then iterate over all elements of stringList in insertion order and print each element on its own line using System.out.println>> %%%)))
add the logic from <addAndPrintMethod.txt> to <<Base_spec.json>> %%%)))
generate a delegate from <<Base_spec.json>> %%%)))
clone the <addAndPrint> method from <<BaseDelegate>> into <<Base>> %%%)))
create a spec file for the <Base> class %%%)))
create a text file <mainMethod.txt> and the content of it should be <<a static main method named main with the standard String array parameter that performs the following steps: first create a new instance of the Base class and store it in a local variable, then assign a descriptive string value such as the words hello from extraString field into the extraString field of that instance, then call the addAndPrint method on that instance so that the extraString value is added to the stringList field and all elements of stringList are printed to standard output>> %%%)))
add the logic from <mainMethod.txt> to <<Base_spec.json>> %%%)))
generate a delegate from <<Base_spec.json>> %%%)))
clone the <main> method from <<BaseDelegate>> into <<Base>> %%%)))
create a spec file for the <Base> class %%%)))
execute the <Base> class %%%)))
