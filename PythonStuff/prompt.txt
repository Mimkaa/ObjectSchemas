create a folder called <FIBONACCI_PROJECT> %%%)))
set the current working directory to be <FIBONACCI_PROJECT> %%%)))
download the library called <net.bytebuddy:byte-buddy:1.15.3> %%%)))
download the library called <org.ow2.asm:asm:9.8> %%%)))
download the library called <org.ow2.asm:asm-tree:9.8> %%%)))
create a base class called <Fibonacci> %%%)))
create a spec file for the <Fibonacci> class %%%)))
create a text file <fibMethod.txt> and the content of it should be <a public method <int fib(int n)> that computes the nth Fibonacci number using an iterative loop and returns the result> %%%)))
add the logic from <fibMethod.txt> to <<Fibonacci>_spec.json> %%%)))
generate a delegate from <<Fibonacci>_spec.json> %%%)))
clone the <fib> method from <<Fibonacci>Delegate> into <Fibonacci> %%%)))
create a spec file for the <Fibonacci> class %%%)))
create a text file <mainMethod.txt> and the content of it should be <a static void main(String[] args) method that calls <fib(9)> on a new <Fibonacci> instance, stores the result in an <int> variable, and prints the result to standard output> %%%)))
add the logic from <mainMethod.txt> to <<Fibonacci>_spec.json> %%%)))
generate a delegate from <<Fibonacci>_spec.json> %%%)))
clone the <main> method from <<Fibonacci>Delegate> into <Fibonacci> %%%)))
create a spec file for the <Fibonacci> class %%%)))
execute the <Fibonacci> class %%%)))
