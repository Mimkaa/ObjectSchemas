create a folder called <PROJECT> %%%)))
set the current working directory to be <PROJECT> %%%)))
download the library called <net.bytebuddy:byte-buddy:1.15.3> %%%)))
download the library called <org.ow2.asm:asm:9.8> %%%)))
download the library called <org.ow2.asm:asm-tree:9.8> %%%)))
download the library called <com.google.code.gson:gson:2.11.0> %%%)))
create a base class called <Base> %%%)))
create a spec file for the <Base> class %%%)))
create a text file <arrayListField.txt> and the content of it should be <<create a new public field named <items> of type <java.util.ArrayList<String>> that will store multiple string elements>> %%%)))
add the field description from <arrayListField.txt> into the spec file <Base_spec.json> for the field named <items> %%%)))
generate a delegate from <Base_spec.json> %%%)))
clone the field <items> from <BaseDelegate> into <Base> %%%)))
create a spec file for the <Base> class %%%)))
create a text file <singleStringField.txt> and the content of it should be <<create a new public field named <extraItem> of type <java.lang.String> that will store a single additional string to be added to the list>> %%%)))
add the field description from <singleStringField.txt> into the spec file <Base_spec.json> for the field named <extraItem> %%%)))
generate a delegate from <Base_spec.json> %%%)))
clone the field <extraItem> from <BaseDelegate> into <Base> %%%)))
create a spec file for the <Base> class %%%)))
create a text file <addAndPrintMethod.txt> and the content of it should be <<create a new public method named <addAndPrint> with no parameters and no return value that does the following steps in order: 1) if the field <items> is null then initialize it as a new <java.util.ArrayList<String>> instance 2) add the value of the field <extraItem> to the <items> list 3) iterate over every element in the <items> list using an enhanced for loop and print each element to standard output using <System.out.println>>> %%%)))
add the logic from <addAndPrintMethod.txt> to <Base_spec.json> %%%)))
generate a delegate from <Base_spec.json> %%%)))
clone the <addAndPrint> method from <BaseDelegate> into <Base> %%%)))
create a spec file for the <Base> class %%%)))
